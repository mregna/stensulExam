Instrucciones:

1) Instalar NodeJS:
    $ sudo apt install nodejs

2) Clonar repositorio de GitHub: https://github.com/mregna/stensulExam.git

3) Por consola, ingresar a la carpeta donde se clono el repositorio y ejecutar para instalar todas las dependecias:
    $ npm i 
    
    Si npm no está instalado, ejecutar :
    $ sudo apt install npm

4) Una vez instaladas las dependecias, en la consola ejecutar el script (ubicado en package.json):
    $ npm run runTest